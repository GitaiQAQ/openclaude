import React from 'react'
import ReactDOM from 'react-dom/client'
import OpenClaudeExample from './OpenClaudeExample'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <OpenClaudeExample />
  </React.StrictMode>,
)
